namespace Aegis.Core
{
    /// <summary>Which campaign / playable AI the player chose.</summary>
    public enum Side
    {
        Good, // AEGIS.2 (protects humans)
        Evil  // X (kills humans)
    }
}
