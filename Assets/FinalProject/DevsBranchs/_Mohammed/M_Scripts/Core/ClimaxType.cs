namespace Aegis.Core
{
    /// <summary>Which mid-fight crisis a campaign uses (GDD §11).</summary>
    public enum ClimaxType
    {
        Reactor, // AEGIS.2 — reactor overload
        Emp      // X — EMP bomb
    }
}
